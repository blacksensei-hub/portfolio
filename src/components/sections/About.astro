---
import Section from '../ui/Section.astro';

interface Props {
  bio: string;
}

const { bio } = Astro.props;
---

<Section id="about" title="About" index="01" layout="split">
  <p class="reveal text-lede max-w-[38ch] lg:pt-2">{bio}</p>
</Section>
