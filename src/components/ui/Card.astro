---
interface Props {
  as?: 'article' | 'div';
}

// Never wraps the whole card in a link; links inside stay separate Tab stops.
const { as: Element = 'article' }: Props = Astro.props;
---

<Element class="rounded-card border-border bg-surface-raised border p-6">
  <slot />
</Element>
